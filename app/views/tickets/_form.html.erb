<%= form_with(model: ticket, local: true) do |form| %>
  <% if ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :meal_preference %>
    <%= form.number_field :meal_preference %>
  </div>

  <div class="field">
    <%= form.label :seat_selection %>
    <%= form.number_field :seat_selection %>
  </div>

  <div class="field">
    <%= form.label :num_luggage %>
    <%= form.number_field :num_luggage %>
  </div>

  <div class="actions">
    <%= button_to 'Add to Cart', line_items_path(flight_id: flight) %>
  </div>
<% end %>

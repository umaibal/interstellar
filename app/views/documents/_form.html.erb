<%= form_with(model: document, local: true) do |form| %>
  <% if document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :document_type %>
    <%= form.number_field :document_type %>
  </div>

  <div class="field">
    <%= form.label :expiration_date %>
    <%= form.date_select :expiration_date %>
  </div>

  <div class="field">
    <%= form.label :passport_number %>
    <%= form.number_field :passport_number %>
  </div>

  <div class="field">
    <%= form.label :drivers_licence_number %>
    <%= form.number_field :drivers_licence_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
